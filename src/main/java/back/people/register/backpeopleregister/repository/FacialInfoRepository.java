package back.people.register.backpeopleregister.repository;

import back.people.register.backpeopleregister.model.FacialInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FacialInfoRepository extends JpaRepository<FacialInfo, String> {
}
